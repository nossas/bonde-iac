name: "api-data"
namespace: "bonde-org"
replicas: 1
container:
  image: "nossas/bonde-an-web-fastapi:latest"
  command: 
    - "uvicorn"
    - "main:app"
    - "--host"
    - "0.0.0.0"
    - "--port"
    - "8000"
  port: 8000
  liveness_probe_path: null
  readiness_probe_path: null
  env_from_secret:
    DATABASE_URL: "bonde-database-url-v2"
  resources:
    requests:
      memory: "200Mi"
      cpu: "50m"
    limits:
      memory: "500Mi"
      cpu: "200m"
service:
  type: "ClusterIP"
  port: 80
  target_port: 8000
ingress:
  enabled: false
labels:
  component: "backend"
  app: "api-data"